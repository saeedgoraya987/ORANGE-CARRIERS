[phases.setup]
nixPkgs = [
  (python311.withPackages (ps: with ps; [
    pip
    requests
    selenium
    # Add your other packages from requirements.txt here
  ]))
  "chromium"
  "chromedriver"
  "ffmpeg"
]

[phases.install]
cmds = [
  # No pip commands needed - packages are already installed
  "echo 'Packages installed via Nix'"
]

[start]
cmd = "python bot.py"
